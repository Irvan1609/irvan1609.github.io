{
  "name": "statistical-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:syntax": "node --check src/main.js && node --check src/ral.js && node --check src/rak-dnd.js && node --check src/number-format.js && node --check src/result-export.js",
    "test:ui": "node scripts/check-ui-contract.mjs",
    "test:dist": "node scripts/check-dist.mjs",
    "verify": "npm run check:syntax && npm run test:ui && npm run build && npm run test:dist"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "exceljs": "^4.4.0",
    "jstat": "^1.9.6"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.0.0",
    "vite": "^7.1.3"
  }
}
