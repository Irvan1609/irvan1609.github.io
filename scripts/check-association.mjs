import assert from 'node:assert/strict';
import fs from 'node:fs';
import {correlation,correlationCritical,correlationCI,coefficientCI,pathAnalysis,numericRows} from '../src/association-engine.js';
import {renderAssociation} from '../src/association-workflow.js';
const a=JSON.parse(fs.readFileSync(new URL('./association-reference.json',import.meta.url)));
const near=(a,b,t=1e-7)=>assert(Math.abs(a-b)<t*Math.max(1,Math.abs(b)),`${a} != ${b}`);
for(const method of ['pearson','spearman']){const c=correlation(a.rows,method);c.pairs.forEach((p,i)=>{near(p.r,a[method][i][0]);near(p.p,a[method][i][1]);assert(p.holm>=p.p&&p.holm<=1);});}
const p=pathAnalysis(a.rows);near(p.r2,a.r2);p.effects.forEach((e,i)=>{near(e.direct,a.beta[i]);near(e.se,a.se[i]);near(e.p,a.p[i]);near(e.vif,a.vif[i]);assert(1/e.vif>0&&1/e.vif<=1+1e-10);near(e.direct+e.indirect.reduce((s,v)=>s+v,0),e.total);});near(p.residual**2,1-p.r2);
const expectedF=(p.r2/p.p)/((1-p.r2)/p.df);near(p.modelF,expectedF);assert(p.modelP>=0&&p.modelP<=1);assert.equal(p.modelP<.05,p.modelF>0&&p.modelP<.05);
near(correlationCritical(27,.05),.3809,.00005);near(correlationCritical(27,.01),.4869,.00005);
const fisher=correlationCI(.5,30,.05);near(fisher[0],.17043136511180015);near(fisher[1],.7289585563883555);assert(fisher[0]<.5&&fisher[1]>.5);assert.deepEqual(correlationCI(1,30,.05),[1,1]);assert.deepEqual(correlationCI(-1,30,.05),[-1,-1]);assert.throws(()=>correlationCI(.5,3,.05));
const betaCI=coefficientCI(.5,.1,20,.05),betaCI99=coefficientCI(.5,.1,20,.01);near(betaCI[0],.29140365940315094);near(betaCI[1],.7085963405968491);assert(betaCI99[0]<betaCI[0]&&betaCI99[1]>betaCI[1]);assert.throws(()=>coefficientCI(.5,-.1,20,.05));assert.throws(()=>coefficientCI(.5,.1,0,.05));
assert.throws(()=>correlation([[1,2],[1,3],[1,4]]));assert.throws(()=>correlation([]));assert.throws(()=>pathAnalysis(a.rows.map(r=>[r[0],r[1],2*r[1]])));assert.throws(()=>numericRows({headers:['X'],rows:[[1]]},[0,0],Number));assert.throws(()=>numericRows({headers:['X'],rows:[['bad']]},[0],Number));near(correlation([[1,4],[1,4],[2,2],[3,1]],'spearman').matrix[0][1],-1);
const huge=Number.MAX_VALUE/4,hugeRows=[[-huge,-huge],[-huge/2,-huge/2],[huge/2,huge/2],[huge,huge]];near(correlation(hugeRows).matrix[0][1],1);assert(Number.isFinite(correlation(hugeRows).matrix[0][1]));
const html=renderAssociation(correlation(a.rows),['TiTa','DiBa','BoPa','PaTo'],'correlation',.05);assert.equal((html.match(/<sup>/g)||[]).length,10);assert(html.includes('segitiga atas'));assert(html.includes('p Holm'));assert(html.includes('1.00<sup>**</sup>'));assert(html.includes('Batas bawah CI 95%'));assert(html.includes('transformasi Fisher z'));
const spearmanHtml=renderAssociation(correlation(a.rows,'spearman'),['TiTa','DiBa','BoPa','PaTo'],'correlation',.05);assert(!spearmanHtml.includes('Batas bawah CI'));assert(spearmanHtml.includes('pendekatan t'));
const rows27=Array.from({length:27},(_,i)=>[i+1,(i+1)*2+((i%3)-1)]),html27=renderAssociation(correlation(rows27),['X','Y'],'correlation',.05);assert(html27.includes('0.3809'));assert(html27.includes('0.4869'));
const pathHtml=renderAssociation(p,['Produksi','TiTa','DiBa','BoPa'],'path',.05),pathHtml99=renderAssociation(p,['Produksi','TiTa','DiBa','BoPa'],'path',.01);assert(pathHtml.includes('Koefisien lintas terstandar'));assert(pathHtml.includes('Dekomposisi korelasi'));assert(pathHtml.includes('koefisien residual'));assert(pathHtml.includes('F model'));assert(pathHtml.includes('p model'));assert(pathHtml.includes('seluruh koefisien lintas prediktor secara simultan'));assert(pathHtml.includes('Batas bawah CI 95%'));assert(pathHtml.includes('distribusi t dengan db residual'));assert(pathHtml.includes('Tolerance'));assert(pathHtml.includes('1/VIF'));assert(pathHtml99.includes('Batas bawah CI 99%'));
console.log('Pearson/Spearman, overflow-safe scaling, Fisher correlation CI, agronomy-style matrix notation, r critical N=27, standardized path coefficients/SE/p/VIF/tolerance, overall path-model F test and 95/99% t confidence intervals match references; path decomposition and residual identity passed.');
